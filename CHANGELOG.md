# Card-Share 專案更新日誌 (Changelog)

本文件用於記錄專案的所有更新內容、功能新增與修復。

---

## [2026-06-02] - 音效整合、首頁圖案與遊戲說明頁面製作

### 新增 (Added)
- **音效管理系統 (`AudioManager.cs`)**：
  - 建立了一個 Singleton（單例模式）的 AudioManager，統一控制遊戲中所有的背景音樂 (BGM) 與音效 (SFX)。
  - 支援背景音樂循環播放（包含暫停、恢復與音量調整功能）。
  - 支援多發牌音效的隨機播放機制（自動隨機挑選發牌音效 1~3 播放，增加真實感）。
  - 預留了答對、答錯、按鈕點擊與放牌的音效欄位與 API。
- **音訊資源匯入**：
  - 在 `Assets/` 目錄下建立了 `Audio/` 資料夾，並將所有音訊資源（背景音樂.mp3、答對音效.mp3、發牌音效 1~3.m4a）匯入專案中。
- **首頁清新可愛風格背景圖 (`game_start_background.png`)**：
  - 生成了延續說明頁面清新綠色、含可愛白色吉祥物的首頁背景圖，存放在 `Assets/Sprites/` 中。
- **說明頁面圖片 (`game_instruction.png`)**：
  - 將設計圖轉換並匯入至 `Assets/Sprites/`。

### 修改 (Changed)
- **遊戲管理器 (`GameManager.cs`) 整合音效**：
  - **發牌音效**：在 `GeneratePuzzle()` 產生新題目時觸發播放。
  - **放牌音效**：在玩家將手牌放入 Slot 槽 (`PlaceCardIntoSlot()`) 時觸發播放。
  - **答對音效**：在判斷式正確且達到目標數值時觸發播放。
  - **答錯音效**：在判斷式結果不符合目標數值時觸發播放。
  - **按鈕音效**：在首頁點擊開始、選擇難度等按鈕切換介面時觸發播放。
- **首頁選單控制器 (`MainMenu.cs`) 整合說明頁面邏輯**：
  - 新增對 `instructionPanel` 遊戲對象的參考，並於 `Start()` 時預設隱藏。
  - 新增說明按鈕 `openInstructionButton` 與關閉按鈕 `closeInstructionButton` 的點擊傾聽器，控制面板顯示與隱藏，並播放按鈕點擊音效。

---

## [工作進度確認]
- [x] 音樂與音效資源整合
- [x] 首頁圖案設計與替換 (已提供 Sprite 與複製檔案)
- [x] 遊戲說明頁面邏輯完成 (已匯入 Sprite 且程式控制程式碼已寫好)
